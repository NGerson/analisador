- name: Implantar no servidor
  on:
     push:
      branches:
      - main
        with:
          # IP ou hostname do seu VPS (você pode ter isso em outro secret, como SSH_HOST)
          host: [31.97.95.109]

          # Usuário que você está usando (no seu caso, provavelmente 'root')
          username: root

          # *** ESTE É O PONTO CRÍTICO: Referência ao Secret com a CHAVE PRIVADA ***
          key: ${{ secrets.SSH_PRIVATE_KEY }} 

          port: 22

          # O que a ação deve executar
          script: |
            # ... seus comandos de deploy (cd, git pull, docker-compose, etc.)